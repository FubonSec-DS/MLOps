# Single runtime role for all SELECT, INSERT, UPDATE, and DELETE operations
DB_USER=DS_MASK
DB_PASSWORD=
DB_DSN=sndmp-scan.fbs100.campus:5211/SNDMP_USER_DS

# Table schemas and the sampling table name are configured in config.yaml.
