這個專案的工作主題可以分成: 
1. 資料準備: 
    - 所有 runtime 查詢與寫入都使用 `DS_MASK`。
    - 完整母體固定使用 `S_IANLEONG.MLOPS_POPULATION`。
    - 特徵表位於 `DS_SEC`。
    - 測試訓練母體快照寫入 `S_IANLEONG.MLOPS_POPULATION_SAMPLING_TEST`。
2. 使用 `DS_MASK` 對完整母體進行 deterministic hash 抽樣。
    - mlops_population.SEGMENT 只會是「潛客」或「非潛客」。
    - 模型使用的 POPULATION 與來源 SEGMENT 是不同概念：一個 POPULATION 可以包含一個或多個 SEGMENT。
        - 「不分潛客」包含「潛客」與「非潛客」。
        - 子 population「潛客」或「非潛客」各自只包含同名 SEGMENT。
    - 對於每一個月(yyyymm)都抽 40 萬筆，作為「訓練母體」
        - 抽樣筆數會因為 `src/mlops/products/config.py` 中的 population 設定而不同。
        - "不分潛客"是 40 萬；"潛客"、"非潛客"則分別都是 15 萬。 
    - 測試產出表格名稱為 `MLOPS_POPULATION_SAMPLING_TEST`。
    - 表格欄位為 `CUSTOMER_ID, YYYYMM, PRODUCT, SOURCE_SEGMENT, Y, ELIGIBILITY_TAG`。
3. 抽完這 40 萬之後要再抽樣一次，抽成只有 10 萬筆資料，作為 pretrain model 的母體。
    - 抽樣方法: 先保留 y=1 的數據，其餘用抽樣補足到總樣本數為 10 萬筆
4. 開始 pretrain，目的是要找出前 100 個最重要的特徵，作為正式訓練時的特徵。 
    - pretrain 之前以母體為主表，LEFT JOIN `DS_SEC` 特徵表。
5. 開始正式 training，以訓練母體為主表 LEFT JOIN 前 100 個重要特徵。
